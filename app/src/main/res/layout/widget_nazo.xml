<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget layout.

  The ambient animation is a ViewFlipper sitting BEHIND the text in a FrameLayout.
  ViewFlipper is one of the few animating views RemoteViews supports, and its
  flipping is driven by the system rather than by any process of ours, so the
  widget animates with no service, no alarm and no work while the screen is off.

  The three children are static drawables; NazoWidgetProvider sets them to the
  phase frames matching the user's chosen background style. Text sits on top in
  the same FrameLayout so it is never obscured by the animation.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg">

    <ViewFlipper
        android:id="@+id/widget_ambient"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:autoStart="true"
        android:flipInterval="3000"
        android:inAnimation="@android:anim/fade_in"
        android:outAnimation="@android:anim/fade_out">

        <ImageView
            android:id="@+id/widget_ambient_a"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="@null" />

        <ImageView
            android:id="@+id/widget_ambient_b"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="@null" />

        <ImageView
            android:id="@+id/widget_ambient_c"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="fitXY"
            android:contentDescription="@null" />
    </ViewFlipper>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_title"
            android:textColor="#7FD8A4"
            android:textSize="11sp"
            android:textStyle="bold"
            android:letterSpacing="0.12" />

        <TextView
            android:id="@+id/widget_streak"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#F2F7F4"
            android:textSize="18sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:shadowColor="#CC101A15"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="6" />

        <TextView
            android:id="@+id/widget_daily"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="13sp"
            android:maxLines="1"
            android:shadowColor="#CC101A15"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="6" />
    </LinearLayout>
</FrameLayout>
